.modal-body
    input.form-control(type='text', #input, [(ngModel)]='value', (keyup.enter)='save()', autofocus)

.modal-footer
    button.btn.btn-primary((click)='save()') 保存
    button.btn.btn-secondary((click)='close()') 取消
