.modal-header
    h5 等待输入按键

.modal-body
    .input
        .stroke(*ngFor='let stroke of value', [@animateKey]='true') {{stroke}}

    .timeout
        div([style.width]='timeoutProgress + "%"')

.modal-footer
    button.btn.btn-primary((click)='close()') 取消
